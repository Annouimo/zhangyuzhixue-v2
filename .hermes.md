# 章鱼智学项目规范

## 临时文件纪律

Agent 在工作过程中会创建大量文件，其中一部分是最终交付物，另一部分只是通往交付物的中间产物（调试脚本、实验代码、临时输出、测试生成的脏数据等）。为了不让项目目录被临时文件污染，遵守以下规则：

### 规则 1：临时文件必须写到一个位置

所有临时性文件必须写入 `.hermes/tmp/` 目录下，不得散落在项目目录各层。

包括但不限于：
- 用于验证想法的一次性调试脚本
- 编译/转换中间产物（如临时 PDF、临时图片裁剪）
- 对比用的代码片段
- 临时数据库文件或测试数据
- 未被设计文档定义的实验性代码

### 规则 2：Permanent vs Temporary 的判断标准

| 判断依据 | 永久（放设计指定路径） | 临时（放 `.hermes/tmp/`） |
|----------|----------------------|--------------------------|
| 设计文档有定义 | ✅ 交付物路径 | ❌ |
| 仅用于验证当前步骤 | ❌ | ✅ 临时区 |
| 测试框架中注册的测试 | ✅ `tests/` | ❌ |
| 手写的一次性数据对比脚本 | ❌ | ✅ 临时区 |
| 最终要进 PR 的 | ✅ | ❌ |
| 纯辅助理解/调试 | ❌ | ✅ 临时区 |

### 规则 3：任务结束时清理

每个任务完成的最后一步，必须确认 `.hermes/tmp/` 目录下的文件是否还有用。无用的直接删除。

如果临时文件内容有意义（比如可以作为参考文档后续复用），应当移到项目文档目录的合适位置，而不是留在 `.hermes/tmp/` 中。

### 规则 4：不再产生新的项目根目录文件

不得在项目根目录创建新文件（`.gitignore`、`README.md`、`pyproject.toml` 等必要的项目配置除外）。所有项目文件按设计文档的目录结构写入对应子目录。

## 其他

- 需要用户确认是否清理的临时文件，先在对话框中列出清单供审批
- `.hermes/tmp/` 已加入 `.gitignore`（如果尚未加入，创建时确认）
- `requirements.txt` 如果是由 agent 生成的，写入 `.hermes/tmp/` 而非根目录
