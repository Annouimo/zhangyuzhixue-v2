# 章鱼智学 · 项目架构与技术规范

> 本文档定义项目的整体架构、数据流规范、HTML 原型 ↔ Flutter 代码对照关系、页面设计说明、以及 Flutter 代码组织规范。
>
> 本文档是统一的主文档，内容覆盖产品设计视角和开发实现视角。原独立文档 `UI说明.md` 已合并至此。

---

## 一、整体架构

```
┌──────────────────────────────────────────────────────────────────┐
│                    客户端 (Flutter)                                │
│                                                                  │
│  ┌───────────┐  ┌───────────┐  ┌──────────────────────────────┐ │
│  │  UI 层     │  │ Repository │  │ SQLite (drift)                │ │
│  │ (Widget)   │◀─│ (数据拼装)  │◀─│ ├─ assets 预置题库 (只读)   │ │
│  └───────────┘  └───────────┘  │ └─ 用户数据 (读写, 本地优先)   │ │
│                                 │       │                        │ │
│  ┌───────────┐                  │ sync_queue → 推送服务器        │ │
│  │ 版本检查   │──→ 版本落后 → 提示用户去商店更新                    │ │
│  └───────────┘                                                   │ │
└──────────────────────────────────────────────────────────────────┘
                                                             │ HTTPS
┌────────────────────────────────────────────────────────────┼──────┐
│                    服务器 (Django)                           │      │
│                                                            ▼      │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │  旧版 API (运行中) → 逐步重构为新版 Django                  │   │
│  │  提供接口：用户数据接收 + 版本号检查                        │   │
│  └────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────┘
```

### 数据流

```
  Django DB
     │
     ▼
  Django REST API  ←── 服务器（旧版逐步过渡到新版）
     │
     ├→ 版本号检查：GET /api/sync/question-bank-version
     │  响应：{ "version": 3, "force_update": false }
     │
     └→ 用户数据接收：POST /api/sync/push
     │
     ▼
  SQLite (drift)
     ├─ assets 预置数据库：题库、步骤、知识卡片（App 自带，首次安装复制到应用目录）
     └─ 用户数据：提交、评分、组卷（本地优先，队列同步到服务器）
     │
     ▼
  Repository 层 (按 UI 需求组合数据)
     │
     ▼
  Flutter UI Widget (展示 + 交互)
```

**核心原则：**
- Repository 按 UI 页面需求组织数据，不暴露数据库表结构
- Drift 模型 = 数据库表的一行（原始字段）；Repository 模型 = UI 要的样子（已拼装/计算）
- 两者不同，不强行统一

---

## 二、页面导航关系

```
                    login → register
                        │
              ┌─────────▼─────────┐
              │   MainShell       │
              │ 首页/作业/讲义/我的 │
              └─────────┬─────────┘
                        │
        ┌───────────────┼───────────────────┐
        ▼               ▼                   ▼
  ┌─────────┐    ┌───────────┐      ┌───────────┐
  │ 首页     │    │ 作业       │      │ 讲义       │
  └────┬────┘    └─────┬─────┘      └─────┬─────┘
       │               │                  │
       ├──→ recommend  ├──→ homework_detail  ├──→ lecture_chapters
       └──→ paper_builder   └──→ solve        └──→ lecture_content
                                   │
                             ┌─────┴─────┐
                             │ paper_     │
                             │ quicklook  │
                             └─────┬─────┘
                                   │
                             ┌─────▼─────┐
                             │ answer_    │
                             │ sheet     │
                             └───────────┘
```

所有子页面均无底部导航。CSS `body.has-nav` 控制 padding。

---

## 三、页面设计说明

### 3.1 导航与页面结构

#### 一级页（底部导航）

| 页面 | 路径 | 说明 |
|------|------|------|
| 作业 | `homework.html` | 作业列表 + 讲义入口 |
| 推荐 | `recommend.html` | 智能推荐 / 偏好推荐双模式 |
| 组卷 | `exam.html` | 新组卷 + 三个功能入口 |
| 我的 | `profile.html` | 个人信息 + 功能入口列表 |

#### 二级页面一览

| 页面 | 路径 | 说明 |
|------|------|------|
| 解题模式 | `solve.html` | 逐步骤解题 |
| 讲义课程 | `lecture_courses.html` → `lecture_chapters.html` → `lecture_content.html` | 三级下钻 |
| 智能组卷 | `paper_auto.html` | 筛选 + 题型 stepper + 难度调优 |
| 自主选题 | `paper_pick.html` | 筛选 + 逐题勾选，底部固定条 |
| 预览（自己） | `paper_quicklook.html` | 公开开关 + 删除 |
| 预览（他人） | `paper_quicklook_other.html` | 点赞 + 收藏 |
| 我的组卷 | `paper_history.html` | 列表 + 公开/私密开关 |
| 发现组卷 | `paper_explore.html` | 4种排序 + 点赞 + 收藏 |
| 我的收藏 | `paper_favorites.html` | 收藏的他人试卷 |
| 快对答案 | `answer_sheet.html` | 答案速览列表 |
| 偏好管理 | `preference_list.html` → `preference_edit.html` | 列表 + 编辑 |
| 首次引导 | `preference_welcome.html` | 注册后无偏好时跳转 |
| 学习统计 | `statistics.html` | 概览 + 4 个图表卡片 |
| 我的页面 | `profile.html` | 个人信息 + 功能入口列表 |
| 编辑资料 | `profile_edit.html` | 个人资料编辑 |
| 成就 | `achievement.html` | 成就列表 |
| 等级 | `level_detail.html` | 等级进度 |
| 积分流水 | `points.html` | 积分记录 |
| 做题历史 | `question_history.html` | 历史记录 |
| 关于 | `about.html` | 版本信息 |

### 3.2 解题模式

`solve.html` — 解题步骤状态机。

#### 完整交互序列

```
选填题：
  [题目显示]
  ↓
  5s 冷却 → 按钮禁用 + 小字倒计时（"⏳ 还剩 N 秒可提交"）
  ↓
  按钮恢复可用 → 用户提交答案
  ↓（选填题无逐步骤反馈）
  自动展示结果（正确/错误 + 解析）
  ↓
  自动进入下一题 或 结束流程
  ↓
  计时重置

解答题（N 步）：
  [当前步卡片显示]
  ↓
  5s 冷却 → 箭头按钮禁用 + 小字倒计时
  ↓
  箭头恢复可用 → 用户点击箭头 ▶
  ↓
  解析内容展开
  ↓
  反馈按钮出现：「全对」「过程对计算错」「不会」
  ↓
  用户点击一个反馈按钮
  ↓
  下一步卡片显示 → 计时重置
  ↓
  （重复直到最后一步）
  ↓
  最后一步反馈 → 恭喜完成（追加在下方）
  ↓
  5 星评分弹出（可选）
  ↓
  「下一题」按钮 或 返回列表
```

#### 防刷倒计时交互规则

- **计时起点**：题目显示（选填）/ 当前步卡片显示（解答），二者统一
- **冷却时长**：5 秒（统一，不区分题型）
- **禁用对象**：选填题→「提交」按钮；解答题→箭头按钮
- **禁用形态**：按钮 `opacity: 0.4; cursor: not-allowed`；按钮下方显示 `⏳ 还剩 N 秒可提交`
- **恢复时机**：5 秒到时，按钮恢复可用，倒计时文字消失
- **重置时机**：进入下一状态时（下一题 / 下一步卡片显示）
- **无额外弹窗/Toast 提示**（视觉禁用 + 小字足够，不干扰读题）

#### 知识卡片交互

- 步骤标题旁的知识标签可点击
- 点击弹出知识卡片弹层（LaTeX 渲染）
- 点击弹层外部关闭

#### 评分交互

- 最后一步完成 → 弹出 5 星评分
- 评分结果用于算法分对比（用户自评 vs 算法预估）
- 评分后展示下一题按钮或结束

### 3.3 讲义

`lecture_courses.html` → `lecture_chapters.html` → `lecture_content.html`

- 三级下钻：课程列表 → 章节目录 → 正文
- 底部固定翻页栏（上一页/下一页 + 页码 + 回到目录）
- 内容区内嵌 LaTeX 公式渲染
- 讲义中出现的知识标签可点击，弹出知识卡片弹层（复用解题模式的弹层）

### 3.4 组卷系统

#### 页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 智能组卷 | `paper_auto.html` | 筛选 + 题型 stepper + 难度调优 |
| 自主选题 | `paper_pick.html` | 筛选 + 逐题勾选，底部固定条 |
| 预览（自己） | `paper_quicklook.html` | 公开开关 + 删除 |
| 预览（他人） | `paper_quicklook_other.html` | 点赞 + 收藏 |
| 我的组卷 | `paper_history.html` | 列表 + 公开/私密开关 |
| 发现组卷 | `paper_explore.html` | 4种排序 + 点赞 + 收藏 |
| 我的收藏 | `paper_favorites.html` | 收藏的他人试卷 |
| 快对答案 | `answer_sheet.html` | 答案速览列表 |

#### 筛选模块交互

筛选模块在 `paper_auto`、`paper_pick`、`recommend` 三页共用一套交互：
- 学科、难度（5段滑块）、题型（选/填/解）、分值范围
- 筛选结果实时更新计数
- 过滤条件可保存为预设（偏好推荐）
- 自主选题跨筛选保留：独立维护 `Set<int> selectedIds`

#### 难度调优（智能组卷）

- 题型默认值（选10填5解6）及高考参考难度从 DB 读取
- 难度调优上下限 = 当前筛选池实际极值，动态变化
- 5段分段标签方案见 [8.3 难度与计算量分段映射](#83-难度与计算量分段映射)

#### 公开/点赞/收藏

- 自己的试卷可开关公开/私密
- 他人的试卷可点赞、收藏
- 点赞、收藏需服务端接口（多人操作）

### 3.5 学习偏好

#### 页面

| 页面 | 路径 | 说明 |
|------|------|------|
| 偏好管理 | `preference_list.html` | 列表 + 编辑/删除 |
| 偏好编辑 | `preference_edit.html` | 新建/编辑 |
| 首次引导 | `preference_welcome.html` | 注册后无偏好时跳转至此 |
| 推荐页 | `recommend.html` | 🔮智能推荐 / 📋偏好推荐 |

#### 推荐模式切换

- 登录/注册成功 → 检查 `preference.count` → 0 则跳转 `preference_welcome.html`
- 推荐页默认模式：做题<5题且有偏好 → 偏好推荐；做题≥5 → 智能推荐

### 3.6 学习统计

`statistics.html` — 概览卡片 + 全局时间范围切换 + 4 个图表卡片。

#### 时间范围切换

5 个 pill 按钮全局切换，影响除概览外的所有图表数据：

| 选项 | 值 | 每日学习显示模式 |
|------|-----|----------------|
| 近一周 | 7天 | 水平条形图（天/格） |
| 近一月 | 30天 | 7行周历（天/格） |
| 近三月 | 90天 | 7行周历（天/格） |
| 近一年 | 365天 | 周/格贡献图 |
| 全部 | 动态 | 月/格 |

#### 自适应粒度规则

```
天数 ≤ 14   → 水平条形图
15 ≤ 天数 ≤ 90  → 7行周历
91 ≤ 天数 ≤ 730 → 周/格贡献图
天数 > 730      → 月/格
```

染色 0~3 级按当期内最大值归一化。

#### 图表卡片

4 个图表卡片（具体图表类型待定），数据维度包括：正确率趋势、积分趋势、题型分布、每日题量。

### 3.7 退出页面评价

弹出时机：返回按钮 20%概率（`solve`/`paper_quicklook`/`lecture_content`），同页 24h 冷却，停留>30s 触发。

- 弹层内容：5 星评分 + 文案
- 冷却机制：存本地 SharedPreferences

---

## 四、HTML 原型标记体系

### 4.1 标记总览

| 属性 | 含义 | 示例 | 对应 Flutter 层 |
|------|------|------|----------------|
| `data-db` | 📖 只读显示 | `data-db="user.getInfo.realName"` | Repository 方法返回值 |
| `data-db-bind` | 🔄 双向绑定 | `data-db-bind="auth.login.username"` | TextEditingController + 初始值 |
| `data-db-action` | ✏️ 写入操作 | `data-db-action="rating.submit()"` | Repository 写入方法 |
| `data-db-loop` | 🔁 列表循环 | `data-db-loop="assign.getPending"` | ListView.builder 数据源 |
| `data-db-empty` | 📭 空状态文案 | `data-db-empty="暂无待办作业"` | 列表为空时的占位 |

### 4.2 路径命名规范

```
格式：{repo缩写}.{方法名}[].{字段名}
```

- 首段：Repository 类名去掉 `Repository` 后首字母小写（`assign` ← `AssignmentRepository`）
- 中段：Repository 的方法名，camelCase
- `[]`：列表中的单个元素
- 尾段：返回对象的字段名，camelCase

**示例：**

```
data-db="assign.getPending[].title"
         assign  ← AssignmentRepository
                 getPending  ← getPending()
                          title  ← AssignmentSummary.title

data-db-action="exam.downloadPdf(1)"
                exam  ← ExamRepository
                     downloadPdf  ← downloadPdf()
```

### 4.3 标记颜色辅助（仅在原型中）

`styles.css` 中对标记了 `data-db` 的元素加了背景高亮，仅用于原型开发阶段：

```
[data-db]       → 浅黄底色 + 橙色虚线下划线
[data-db-loop]  → 蓝色虚线边框
```

### 4.4 SharedPreferences 标记说明

SharedPreferences **不纳入 `data-db` 标记体系**。原因：
- SharedPreferences 不直接驱动 UI 渲染（token、冷却时间、版本弹窗记录均为后台状态）
- `data-db` 的意义是「指着 HTML 元素说数据来自 Repository 的哪个方法」，原型评审者不需要看 key 名

SharedPreferences key 在 Flutter 代码中各页面的 `_Prefs` 类中声明，详见 [7.2 页头编译常量规范](#72-页头编译常量规范)。

---

## 五、Repository 层规范

### 职责

Repository 为 UI 层提供组合好的数据对象，不直接暴露数据库表结构。

```
❌ 错误：AssignmentRepository.getAll()
         + ClassCourseAssignmentRepository.getByXxx()
         + CourseRepository.getById()
   → UI 层自己 join 3 张表

✅ 正确：AssignmentRepository.getPending()
   → 返回已经包含 title/courseName/doneCount/deadline 等所有字段的对象
```

### 与数据库模型的关系

```
Drift Table (存)   →  数据类 = 数据库表的一行，含主键/外键/原始字段
Repository (用)    →  数据类 = UI 需要的样子，已拼装/已计算
```

两者用途不同，各自独立。Repository 方法内部负责：查 DB → JOIN/计算 → 拼装 → 返回 Repository 模型。

### 简单/复杂分离

Repository 文件中的方法分为两类：

| 类型 | 特征 | 维护频率 | 涉及文件 |
|------|------|---------|---------|
| **简单方法** | 直接读写本地 db，无额外计算 | 写一次不再动 | 全部 13 个文件 |
| **复杂算法** | 需聚合/过滤/推荐/状态计算 | 算法持续迭代 | 5 个文件尾部私有类 |

复杂逻辑写在文件尾部的私有类中（如 `_PointsCalculator`），**仅供本文件 Repository 内部调用**，不对外暴露。页面开发者只需 import Repository 类，不需要知道私有类的存在。

```dart
class ExamRepository {
  // 简单方法（直接读写本地 db）
  static Future<List<ExamSummary>> getMyExams() async { ... }

  // 复杂方法（委托给私有类）
  static Future<PoolStats> getPoolStats(SearchFilters f) async {
    return _ExamFilterEngine().compute(f);
  }
}

// 文件尾部私有类
class _ExamFilterEngine { ... }
```

### 实现文件

见 `spec/Repository/` 目录下的 13 个 Dart 文件。每个文件包含 Repository 类 + 数据模型类，5 个文件尾部附私有算法类。

| 文件 | Repository | data-db 前缀 | 对应页面 | 尾部算法类 |
|------|------------|-------------|---------|-----------|
| `auth_repository.dart` | AuthRepository | `auth.*` | login, register | — |
| `assignment_repository.dart` | AssignmentRepository | `assign.*` | homework_list, homework_detail, index(待办数) | — |
| `lecture_repository.dart` | LectureRepository | `lecture.*` | lecture_courses, lecture_chapters, lecture_content | — |
| `question_repository.dart` | QuestionRepository | `question.*` | solve(题干), paper_quicklook(做题动作) | — |
| `progress_repository.dart` | ProgressRepository | `progress.*` | solve(解题步骤) | — |
| `rating_repository.dart` | RatingRepository | `rating.*` | solve(评分) | — |
| `sync_repository.dart` | SyncRepository | `sync.*` | sync_queue, profile(副标题) | — |
| `preference_repository.dart` | PreferenceRepository | `preference.*` | preference_*, profile(偏好数), paper_auto/pick(加载预设) | — |
| `user_repository.dart` | UserRepository | `user.*`, `tasks.*`, `points.*`, `app.*` | index(任务卡片), profile, profile_edit, points, level_detail, about, solve(积分行) | `_PointsCalculator` |
| `exam_repository.dart` | ExamRepository | `exam.*` | 所有组卷页面 (auto/pick/quicklook/explore/favorites/history/answer_sheet) | `_ExamFilterEngine` + `_ExamGenerator` + `_ExamPdfService` |
| `recommend_repository.dart` | RecommendRepository | `recommend.*` | recommend | `_RecommendationEngine` |
| `statistics_repository.dart` | StatisticsRepository | `stats.*` | statistics, profile(统计摘要) | `_StatisticsAggregator` |
| `achievement_repository.dart` | AchievementRepository | `achievement.*` | achievement, profile(成就数) | `_AchievementEngine` |

### 文件结构规范

每个 `.dart` 文件按以下顺序排列：

```dart
/// 章鱼智学 — {Domain}Repository
/// data-db: {prefix}.*
/// 对应页面：{页面列表}

// 1. 数据模型类（const + fromJson）
class ModelA { ... }
class ModelB { ... }

// 2. Repository 类（static 方法，throw UnimplementedError）
class {Domain}Repository {
  /// GET /api/xxx/
  static Future<T> methodName() async {
    throw UnimplementedError('{Domain}Repository.methodName');
  }
}

// 3. （可选）私有算法类，仅供本文件内部使用
class _{ComplexEngine} { ... }
```

### 跨页面数据组织

一个页面可引用多个 Repository。例如 profile.html 的数据来自 5 个领域：

```dart
// profile 页面的 ViewModel
final user = await UserRepository.getUserInfo();
final stats = await StatisticsRepository.getOverview();
final prefCount = await PreferenceRepository.getCount();
final achCount = await AchievementRepository.unlockedCount();
final syncStatus = await SyncRepository.hasFailed();
```

不设 ProfileRepository，不制造没有 data-db 对应关系的包装层。

### 缩略名规则

Repository 类名去掉 `Repository` 后首字母小写，即为 data-db 路径的首段和 Flutter 代码中的变量名：

```
AssignmentRepository → assign  ← data-db="assign.*"
ExamRepository       → exam    ← data-db="exam.*"
```

---

## 六、本地数据方案

### 6.1 数据分类

| 类别 | 内容 | 来源 | 同步方式 |
|------|------|------|---------|
| 题库 | 题目、步骤、知识卡片 | App 预置 | 版本号检查 → 提示商店更新 |
| 元数据 | 课程、讲义、作业 | App 预置 | 同上 |
| 用户操作 | 提交记录、反馈、评分 | 本地写入 | 队列推送到服务器 |
| 组卷 | 学生自组试卷 | 本地写入 | 队列推送到服务器 |
| 积分 | 积分余额 | 服务器计算 | 本地缓存显示 |

### 6.2 预置题库数据库

- 开发时用 drift 建好全量表结构，导入数据生成 `.db` 文件
- 放入 Flutter `assets/` 目录
- App 首次启动复制到应用文档目录（`getApplicationDocumentsDirectory()`）
- 后续启动直接使用，无需等待网络

**更新流程：**
- App 启动时调用 `GET /api/sync/question-bank-version`
- 响应：`{ "version": 3, "force_update": false, "message": "新增2025年真题" }`
- 版本落后 → 非强制则右下角提示；强制则弹框引导去应用商店
- 不做增量拉取，不自动下载；更新 = 发新版 App

### 6.3 用户数据同步

**写入流程：**
```
用户操作 → 写入本地 SQLite（即时响应）
         → 追加 sync_queue 表
         → App 启动时/手动触发批量推送
```

**队列表结构：** `entity_type` / `operation_type` / `payload`(JSON) / `status` / `retry_count`

**冲突处理：** LWW（last-write-wins），服务器比较 `updated_at` 覆盖旧值。用户数据均为单人独写，不存在多用户写同一条记录的情况。

### 6.4 与旧版 Django 的适配

旧版 Django 先运行，新版逐步重构。适配点仅在 API 层：

```
旧版 Django API  →  Flutter API 层 (api_client.dart / sync_api.dart)
                    ↑ 这里做字段映射（唯一适配点）
                    ↓ 以下不动
                  SQLite + Repository + UI
```

新版 Django 上线后只改 API 层，其余不动。

### 6.5 SharedPreferences 路径规范

SharedPreferences 用于存储**纯客户端状态**，不经过 Repository 体系（不走数据同步流程）。

#### 使用场景汇总

| 场景 | key 命名 | 对应页面 | 说明 |
|------|---------|---------|------|
| 登录 token | `app_auth_token` | auth, 全局 | 登录成功后存储，登出时清除 |
| 用户信息缓存 | `app_user_cache` | 全局 | JSON，启动时快速展示个人资料 |
| 评价弹窗冷却 | `solve_rating_cooldown_{page}` | solve, paper_quicklook, lecture_content | 同页 24h 内不重复弹出 |
| 版本更新弹窗 | `app_update_last_shown` | 全局 | 上次提示版本更新的时间 |
| 解题防刷上次提交 | `solve_last_submit_{questionId}` | solve | 5s 冷却校验 |
| 新手引导标记 | `{page}_guide_done` | 各页面 | 首次进入新手引导 |

#### key 命名规范

```
{页面缩写}_{用途}
```

- 全局性的 key 以 `app_` 开头
- 页面级的 key 以页面缩写开头
- 需要区分具体对象的 key，在末尾拼唯一标识（如 `{questionId}`）

#### 代码规范

- 每个页面对应的 Dart 文件内声明 `_Prefs` 类，集中管理该页使用的所有 key
- 全局共用的 key 放在 `lib/constants/app_prefs.dart`

```dart
// pages/solve/solve_page.dart 头部
class _SolvePrefs {
  static const String lastSubmit = 'solve_last_submit_{qid}';
  static const String ratingCooldown = 'solve_rating_cooldown';
}
```

不设 HTML 标记，不在原型中标注。

---

## 七、Flutter 代码结构

### 7.1 页面 Dart 文件组织

按照底栏 4 个 Tab 分组，每个 Tab 及其所有子页面归入同一个目录。跨 Tab 复用的页面（solve、preference）独立目录。

```
lib/pages/
├── homework/         # 作业 Tab + 子页面
│   ├── list_page.dart
│   ├── detail_page.dart
│   └── widgets/
│       ├── homework_card.dart
│       └── lecture_entry.dart
├── recommend/        # 推荐 Tab
│   └── recommend_page.dart
├── exam/             # 组卷 Tab + 8 个子页面
│   ├── auto_page.dart
│   ├── pick_page.dart
│   ├── quicklook_page.dart
│   ├── quicklook_other_page.dart
│   ├── history_page.dart
│   ├── explore_page.dart
│   ├── favorites_page.dart
│   ├── answer_sheet_page.dart
│   └── widgets/
│       ├── filter_panel.dart
│       └── exam_card.dart
├── profile/          # 我的 Tab + 子页面
│   ├── profile_page.dart
│   ├── edit_page.dart
│   ├── statistics/
│   │   ├── statistics_page.dart
│   │   └── widgets/         # 4 个图表
│   ├── achievement_page.dart
│   ├── points_page.dart
│   ├── level_detail_page.dart
│   ├── about_page.dart
│   └── question_history_page.dart
├── auth/             # 登录/注册（独立 Tab 外）
│   ├── login_page.dart
│   └── register_page.dart
├── preference/       # 偏好管理（多 Tab 引用）
│   ├── list_page.dart
│   ├── edit_page.dart
│   └── welcome_page.dart
├── solve/            # 核心 — 独立目录（跨 Tab 复用）
│   ├── solve_page.dart
│   ├── _strings.dart        # 本页文案常量
│   ├── _prefs.dart          # 本页 SharedPreferences key
│   └── widgets/
│       ├── feedback_buttons.dart
│       └── knowledge_card_sheet.dart
└── lecture/          # 讲义（从作业 Tab 进入，但独立）
    ├── courses_page.dart
    ├── chapters_page.dart
    └── content_page.dart
```

**跨 Tab 页面的归属规则：**
- 一个页面只属于一个目录，不因被多个 Tab 引用而复制
- `solve/` 独立：作业→解题、组卷→预览→解题 都引用同一个 `solve_page.dart`
- `preference/` 独立：偏好管理从 profile 进入，但推荐页也引用偏好数据
- Widget 引用路径使用相对导入或 `package:` 导入

### 7.2 页头编译常量规范

#### 为什么要做

- 所有显示文字集中到文件头部，改文案不用在 widget 树里翻
- 一目了然本页涉及哪些 SharedPreferences key
- 方便日后的文案审查和多语言

#### 显示文字：`_Strings` 类

每个页面对应的 Dart 文件头部声明一个私有 `_Strings` 类，所有用户可见的文字定义为 `static const String`：

```dart
// 在页面主文件（或独立的 _strings.dart）头部
class _SolveStrings {
  static const String pageTitle = '解题模式';
  static const String feedbackCorrect = '全对';
  static const String feedbackPartial = '过程对计算错';
  static const String feedbackWrong = '不会';
  static const String completeCongrats = '🎉 恭喜完成！';
  static const String cooldownHint = '⏳ 还剩 {n} 秒可提交';  // 格式化
  static const String nextStepHint = '点击箭头进入下一步 ▶';

  static String formatCooldown(int seconds) =>
      cooldownHint.replaceAll('{n}', seconds.toString());
}
```

**全局共享文字**单独放 `lib/constants/app_strings.dart`，如：
- 通用按钮：提交、取消、返回、保存、删除、确认
- 通用提示：加载中、暂无数据、网络错误
- App 名称和品牌文案

#### SharedPreferences key：`_Prefs` 类

同上，每页头部声明一个 `_Prefs` 类：

```dart
class _SolvePrefs {
  static const String lastSubmitPrefix = 'solve_last_submit_';
  static const String ratingCooldown = 'solve_rating_cooldown';

  static String lastSubmitKey(int questionId) =>
      '$lastSubmitPrefix$questionId';
}
```

#### 防刷 5s 常量

`_SolveStrings` 中声明 `cooldownHint` 格式化模板；5s 的数值常量直接写在计时逻辑中（`const Duration(seconds: 5)`），不需要独立抽为常量。

### 7.3 Widget 复用规范

#### 判定规则

```
跨 2 页以上出现 且 语义一致 → 抽出 Widget
```

- **跨 2 页以上**：出现于 2 个或以上页面（包括同一个 Tab 下的不同子页面）
- **语义一致**：交互行为相同、数据结构相同。如果只是长得像但交互不同（如一个点击跳转、一个弹窗），不抽
- **先写内联再抽**：不确定是否会复用时，先内联写，第二次出现时重构抽出

#### 存放目录

```
lib/widgets/
├── common/           # 通用组件（跨域复用）
├── {domain}/         # 领域内复用（如 exam/, solve/）
└── widgets.dart      # barrel export
```

### 7.4 可复用 Widget 汇总清单（当前已知）

以下是根据 HTML 原型初步识别的可复用 Widget，后续在实际开发中补充或调整：

| Widget | 使用页面 | 复用次数 | 归属目录 |
|--------|---------|---------|---------|
| `FilterPanel` | paper_auto, paper_pick, recommend | 3 | `lib/widgets/exam/` |
| `ListItemTile` | profile(4处), preference_list, all group paper lists | 10+ | `lib/widgets/common/` |
| `CardSectionHeader` | 所有带分组标题的卡片区 | 多处 | `lib/widgets/common/` |
| `FeedbackButtons` | solve（全对/过程对/不会） | 1，但逻辑复杂 | `lib/widgets/solve/` |
| `KnowledgeCardSheet` | solve, lecture_content | 2 | `lib/widgets/solve/` |
| `DifficultySlider` | paper_auto, paper_pick | 2 | `lib/widgets/exam/` |
| `EmptyStatePlaceholder` | 所有列表页 | 8+ | `lib/widgets/common/` |

---

## 八、设计系统

### 颜色

| 角色 | 值 | 用途 |
|------|-----|------|
| 主色 | `#4A6CF7` | 按钮、导航高亮、链接 |
| 浅蓝底 | `#EEF1FF` | 知识卡片标签背景、活跃状态 |
| 背景 | `#F5F7FA` | 页面背景 |
| 卡片 | `#FFFFFF` | 卡片背景 |
| 主文字 | `#1A1A2E` | 正文 |
| 副文字 | `#6B7280` | 辅助说明 |
| 成功 | `#10B981` | 已完成状态 |
| 警告 | `#F59E0B` | 进行中状态 |
| 错误 | `#EF4444` | 错误、删除操作 |

### 8.3 难度与计算量分段映射

难度和计算量在数据库中为 0-10 连续值，在 UI 中分为 5 段展示，用于筛选滑块和推荐标签。

#### 难度 5 段

| 段 | 范围 | 标签 | 题型举例 |
|---|------|------|---------|
| 基础 | 0-3 | 基础 | 单选1-3·填空11·解答第一问 |
| 中档 | 3-5 | 中档 | 单选4-6·填空12-13·常规解答 |
| 中难 | 5-7 | 中难 | 选填后半·填空14·解答多步推理 |
| 较难 | 7-8.5 | 较难 | 选填压轴·解答最后两问 |
| 压轴 | 8.5-10 | 压轴 | 解答压轴问·创新综合题 |

**数据依据：** 选择题 Q1-Q10 难度从 0.3 递增到 6.9（选择题不含 7+ 题）；填空题 Q11-Q15 难度从 1.7 到 7.2（仅 3 题落在 7 段）；解答题 Q16-Q21 从 0.0 到 9.7 全覆盖（含多小问）。分段边界在题型构成明显变化处：3 以上选择题不再包揽基础，5 以上解答题比例显著增加，7 以上全是解答题，8.5 以上仅解答题压轴问。

**难度分布（798 题统计）：**

| 段 | 题数 | 占比 |
|-----|------|------|
| 0-3 | 271 | 34% |
| 3-5 | 226 | 28% |
| 5-7 | 361 | 45% |
| 7-8.5 | 107 | 13% |
| 8.5-10 | 19 | 2% |

#### 计算量 5 段

| 段 | 范围 | 标签 | 运算描述 |
|---|------|------|---------|
| 少量 | 0-2 | 少量 | 心算即可，不需动笔 |
| 较少 | 2-4 | 较少 | 简单代入化简，2-3步 |
| 适中 | 4-6 | 适中 | 常规运算量，需完整步骤 |
| 较多 | 6-8 | 较多 | 多步代数运算，需仔细 |
| 繁琐 | 8-10 | 繁琐 | 大量代数变换，需耐心推导 |

**计算量分布（798 题统计）：**

| 段 | 题数 | 占比 |
|-----|------|------|
| 0-2 | 180 | 22.6% |
| 2-4 | 132 | 16.5% |
| 4-6 | 212 | 26.6% |
| 6-8 | 220 | 27.6% |
| 8-10 | 54 | 6.8% |

#### 滑块标记位置

难度和计算量在 UI 滑轨上的标签位置不同：

- **难度**：边界在 30%, 50%, 70%, 85%（即 3/5/7/8.5 对应轨道百分比）
- **计算量**：边界在 20%, 40%, 60%, 80%（等分）

#### 注意事项

- `models.py` 注释写的 `difficulty(1-5)` / `workload(1-5)` 已过时，实际是 0-10 连续值
- 问答 rating 表（QuestionRating）的 difficulty/workload 也是 0-10 连续值
- 难度和计算量高度相关（r=0.808），不存在"高难度低计算量"或"低难度高计算量"的题目
- median 难=5.3，median 计=5.3

### 间距与圆角

| 名称 | 值 |
|------|-----|
| 基础间距 | 16px |
| 卡片圆角 | 12px |
| 按钮圆角 | 8px |
| 最大宽度 | 480px（移动优先） |

---

## 九、工作流程

### HTML 原型 → Flutter 代码

```
1. 设计阶段
   UI_Phase1.canvas（手绘草图）
        │
        ▼
   UI_Phase2/*.html（可交互原型）
        │
        ▼
   评审定稿
        │
        ▼
2. 开发阶段
   Flutter UI Widget → 对照 HTML 布局
   Repository 层     → 对照 data-db 标记 + spec/Repository/*.dart
   SQLite (drift)    → 对照 docs/服务器_数据库结构.md
        │
        ▼
3. 数据对接
   把 repository.dart 中 throw UnimplementedError
   替换为 drift DAO 调用 + API 调用
```

### 修改 HTML 后的同步流程

修改 HTML 中的 `data-db` 路径后，需要同步更新：

```
1. 修改的 HTML 文件            ← data-db 路径
2. 对应的 repository.dart      ← 模型字段名或方法返回结构
3. 所有 HTML 中相同路径的其他页面 ← 全文搜索确保一致性
```

---

> 本文档最后更新：2026-07-07
> 合并了原 `UI说明.md` 的内容。原 `UI说明.md` 已废弃不再维护。
