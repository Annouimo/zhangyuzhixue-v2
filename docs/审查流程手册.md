# 章鱼智学 · 审查流程手册

> 本文档定义项目 Owner 验收审计的完整流程、审计分类、力度要求和常见陷阱。
> 审计由 Hermes agent 执行，加载 skill `project-owner-acceptance` 后按此手册操作。

---

## 一、核心理念

每次审查的目的是：**确认当前交付质量是否达到 Owner 的可接受标准。**

**审计是对话产物——只报告，不修改。** Agent 发现的问题呈现给 Owner 判断，不经指示不修代码、不改文档、不跑数据迁移。

---

## 二、代码全覆盖矩阵

每个代码目录被且仅被一个审计类型覆盖（标 * 的为横切类型，不独占文件夹）。

| 代码路径 | 审计类型 | 说明 |
|----------|---------|------|
| `math_platform/accounts/` | A | 用户认证、权限 |
| `math_platform/assignments/` | A | 作业管理 |
| `math_platform/feedback/` | A | 反馈系统 |
| `math_platform/knowledge/` | A | 知识卡片 |
| `math_platform/lectures/` | A | 讲义管理 |
| `math_platform/notes/` | A | 笔记 |
| `math_platform/questions/` | A | 题库引擎、组卷、PDF生成 |
| `math_platform/tags/` | A | 概念标签 |
| `math_platform/math_platform/` | A | Django项目核心 |
| `math_platform/templates/` | A | Django模板 |
| `math_platform/static/` | A | 静态资源 |
| `flutter_app/pubspec.yaml` | B* | 资产声明文件（非 lib/ 下代码，属 Type B 横切验证项） |
| `flutter_app/assets/db/` | B* | 捆绑数据库文件（验证存在性 + 与 pubspec.yaml 一致） |
| `flutter_app/lib/data/` | B | 数据层 |
| `flutter_app/lib/domain/` | B | 仓库层 |
| `flutter_app/lib/pages/` | C | UI页面 |
| `flutter_app/lib/widgets/` | C | 共享Widget |
| `flutter_app/lib/main.dart + app_theme.dart` | C | 入口+主题 |
| `server/`（全部Django app） | D | 教师端服务端 |
| `landing/`（含 teacher/） | D | 着陆页+教师前端 |
| `.github/` | E* | CI/CD |
| `math_platform/scripts/` | E* | 部署/备份脚本 |

**说明：**
- 路径基于项目根目录 `D:\Hermes\zhangyuzhixue_app_v2`，`math_platform/` 在 `D:\Hermes\math_platform\`
- Type E（部署）和 Type G（横切）是横切类型，不独占文件夹
- `server/scripts/` 归 Type D，其中的备份脚本在 Type E 中也会涉及

---

## 三、审计分类（7 类）

### Type A — 服务端审计

**覆盖文件夹：** `math_platform/accounts/`、`math_platform/assignments/`、`math_platform/feedback/`、`math_platform/knowledge/`、`math_platform/lectures/`、`math_platform/notes/`、`math_platform/questions/`、`math_platform/tags/`、`math_platform/math_platform/`、`math_platform/templates/`、`math_platform/static/`

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| A1 | `02-数据/数据库结构设计.md` | 全部表定义 — 逐字段比对 models.py |
| A2 | `03-服务端/API设计.md` | 全部端点规范 — 逐端点比对 views/urls |
| A3 | `03-服务端/服务端架构.md` | App 划分、认证方案、部署策略 |
| A4 | `03-服务端/PDF方案设计.md` | PDF 签名、渲染模板 |
| A5 | `03-服务端/pdf/test_paper.html` | PDF 渲染模板的 HTML 示例 |
| A6 | `02-数据/学号生成算法.md` | Student.student_id 的 LCG 算法 |
| A7 | `落地实施/方案-同步进度展示.md` | 同步进度展示（涉及后端 API） |
| A8 | `落地实施/方案-用户数据拉取机制.md` | 用户数据拉取方案 |
| — | `验收检查清单.md` | 验收标准 |
| — | `落地实施/Phase-1-服务端全量.md` | 全量功能实施细化 |

**逐文件夹对应 probe：**

| 文件夹 | 设计文档对照源 | Probe 要点 |
|---------|--------------|-----------|
| `accounts/` | A1/A2/A3 | User/Student 模型字段、认证端点（login/register/refresh/token）、权限装饰器 |
| `questions/` | A1/A2/A4/A5 | BaseQuestion 模型字段、题库 API、PDF 生成端点、CustomExam |
| `assignments/` | A1/A2 | Assignment 模型字段、作业 API、发布/提交/批改流程 |
| `knowledge/` | A1 | KnowledgeCard 模型、概念标签关系 |
| `lectures/` | A1/A2 | Lecture 模型字段、讲义 API |
| `feedback/` | A1/A2 | Feedback 模型、反馈提交/统计 |
| `notes/` | A1 | 笔记模型 |
| `tags/` | A1 | ConceptTag 树形结构 |
| `math_platform/` | A3 | settings.py 配置（JWT/CORS/Sentry/DB）、urls.py 路由汇总、middleware |
| `templates/` | A3 | Django admin 模板、home 页面 |
| `static/` | — | 静态文件完整性 |
| **（跨层格式）** | **A2 + Flutter API 消费端** | **API 响应中每个 `url`/`path`/`src`/`href` 类字段，确认格式是否可直接被客户端消费（`Dio.download()` 需绝对 URL、`Image.network()` 需绝对 URL）。同类端点是否一致使用 `build_absolute_uri()`。** |

---

### Type B — Flutter 数据层审计

**覆盖文件夹：** `flutter_app/lib/data/`（api/daos/database/helpers/network/prefs/sync）+ `flutter_app/lib/domain/`

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| B1 | `02-数据/数据库结构设计.md` | 镜像原则 — 比对 Drift schema vs Django models |
| B2 | `02-数据/本地数据架构.md` | 三库分离方案、网络失败处理 |
| B3 | `02-数据/更新机制.md` | 版本号体系、启动流程、强制/非强制规则 |
| B4 | `02-数据/构建脚本设计.md` | assets.db/lectures.db 表结构定义 |
| B5 | `05-Flutter/数据访问层设计.md` | DAO→Database、Repository→DAO 层间契约 |
| B6 | `05-Flutter/Flutter代码规范.md` | Repository 规范、构造注入规则 |
| B7 | `05-Flutter/同步引擎设计.md` | 同步队列状态机、重试策略 |
| B8 | `05-Flutter/图片路由规范.md` | 配图路径规范 |
| B9 | `05-Flutter/Repository/*.dart`（13 个设计稿） | 逐方法比对设计稿签名 vs 实现签名 |
| B10 | `05-Flutter/sync/*.dart`（4 个设计稿） | 同步引擎设计稿 vs 实现代码 |
| B11 | `05-Flutter/helpers/README.md` + `*.dart` | Helper 工具函数的接口定义 |
| B12 | `05-Flutter/components/md-latex-body/README.md` + `md_latex_body.dart` | MdLatexBody 组件规范和使用限制 |
| B13 | `落地实施/方案-同步进度展示.md` | 同步队列状态页的专项设计方案 |
| B14 | `落地实施/方案-用户数据拉取机制.md` | 用户数据拉取流程的专项设计方案 |

**逐文件夹对应 probe：**

| 子文件夹 | Probe 要点 |
|----------|-----------|
| `data/api/` | ApiClient 方法签名 vs 后端 API 端点、错误处理、retry。**对每个从 API 响应中提取的 URL/路径字段，反向验证 API 返回的格式是否匹配消费端的要求（`Dio.download()` 需绝对 URL、`Image.network()` 需绝对 URL）。格式不一致 = 静默下载/渲染失败。** |
| `data/daos/` | DAO CRUD 方法完整性、事务处理、Drift 查询正确性 |
| `data/database/` | Drift schema vs Django models（镜像原则）、migration 版本管理 |
| `data/helpers/` | helper 函数接口 vs 设计稿 |
| `data/network/` | 网络状态监听、离线检测 |
| `data/prefs/` | SharedPreferences 存取正确性 |
| `data/sync/` | 同步队列状态机、冲突解决、重试策略 |
| `domain/`（13 个 Repository） | 方法签名 vs B9 设计稿、构造注入、data-db 绑定 |
| `pubspec.yaml`（横切） | 搜集 `lib/` 下所有 `rootBundle.load('assets/...')` / `AssetImage('assets/...')` 引用 → 交叉验证 `pubspec.yaml → flutter → assets` 声明了每个路径。缺少声明 = 运行时 AssetNotFound 闪退 |

---

### Type C — Flutter UI 审计

**覆盖文件夹：** `flutter_app/lib/pages/`（exam/homework/lecture/profile/solve/statistics）+ `flutter_app/lib/widgets/` + `flutter_app/lib/main.dart` + `flutter_app/lib/app_theme.dart`

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| C1 | `04-UI/页面设计说明.md` | 各页面交互设计、Widget 复用指引 |
| C2 | `04-UI/设计系统.md` | 颜色、难度分段、间距圆角 |
| C3 | `04-UI/data-db标记规范.md` | HTML data-db → Repository 方法映射契约 |
| C4 | **`04-UI/html/*.html`（全部 30 个原型文件）** | UI 的视觉黄金标准 — 逐页面比对 Flutter Widget |
| C5 | `04-UI/html/styles.css` | CSS 变量定义 — 与 Flutter 端设计系统对照 |
| C6 | `01-架构/页面导航.md` | 路由关系、页面清单 |
| C7 | `01-架构/架构概览.md` | 7 层架构与数据流 |
| C8 | `05-Flutter/Flutter代码规范.md` | Widget 组织、路由方案 |
| C9 | `05-Flutter/数据访问层设计.md` | Repository 接口契约 — 验证 UI 的 data-db 绑定 |
| C10 | `05-Flutter/components/md-latex-body/README.md` | MdLatexBody 使用规范 |
| C11 | `05-Flutter/Repository/*.dart`（13 个设计稿） | 确认 UI 数据源来自设计稿定义的 Repository 方法 |
| C12 | `测试策略.md` | UI 阶段该有多少测试（L5 Widget 测试） |

**逐文件夹对应 probe：**

| 子文件夹 | 对应 HTML 原型 | Probe 要点 |
|----------|---------------|-----------|
| `pages/exam/` | `04-UI/html/exam*.html` | 筛选面板、试卷卡片、题目预览、快速浏览 |
| `pages/homework/` | `04-UI/html/homework*.html` | 作业列表、作业详情、提交 |
| `pages/lecture/` | `04-UI/html/lecture*.html` | 课程列表、章节列表、讲义内容渲染 |
| `pages/profile/` | `04-UI/html/profile*.html` | 个人资料、成就、积分、等级、历史记录 |
| `pages/solve/` | `04-UI/html/solve-pages/*.html` | 解题流程（选择/填空/步骤/评价/地图） |
| `pages/statistics/` | `04-UI/html/statistics.html` | 统计图表、热力图、趋势图 |
| 顶层页面 | `04-UI/html/index/login/register/recommend` | 首页、登录、注册、推荐 |
| `widgets/` | 各页面复用组件 | 加载/错误/空三态、冷却计时器、反馈按钮 |

---

### Type D — 教师端审计

**覆盖文件夹：** `server/`（全部 Django app）+ `landing/` + `landing/teacher/`

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| D1 | `06-教师端/教师端功能边界.md` | 学生端 vs 教师端的功能边界定义 |
| D2 | `06-教师端/html/*.html`（10 个文件） | 教师端 UI 原型 |
| D3 | `03-服务端/API设计.md` §六 | 教师 API 端点规范 |
| D4 | `02-数据/数据库结构设计.md` | 教师端涉及的 DB 表 |

**逐文件夹对应 probe：**

| 子文件夹 | Probe 要点 |
|----------|-----------|
| `server/accounts/` | 教师认证、权限模型 |
| `server/courses/` | 教师 API（9 个端点）、serializers、权限控制 |
| `server/interactions/` | 同步 API、PDF 生成 API |
| `server/qbank/` | 题库 API（教师视角） |
| `server/system/` | 系统配置 API |
| `server/templates/` | HTML 模板完整性 |
| `landing/` | 着陆页（index/privacy/terms） |
| `landing/teacher/` | 前端 9 页（classes/detail/index/login/profile/publish/student/students/about） |

---

### Type E — 部署审计

**覆盖文件：** `.github/` + `.gunicorn/` + `server/scripts/`（备份相关）+ `math_platform/scripts/` + 服务器配置

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| E1 | `03-服务端/服务端架构.md` §五 | 部署方案要点 |
| E2 | `备份方案.md` | 备份策略和 crontab 配置 |
| E3 | `落地实施/Phase-1.5-Staging部署.md` | Staging 部署方案 |
| E4 | `落地实施/Phase-6-用户迁移上线.md` | 正式上线方案 |

**检查要点：**

| 检查项 | 方法 |
|--------|------|
| nginx + gunicorn 运行 | `systemctl status gunicorn` + `curl <domain>/api/v1/` |
| 静态文件直出 | `curl <domain>/static/` |
| 认证守卫 | `curl <domain>/api/v1/user/me/` → 401 |
| 维护页面回退 | `systemctl stop gunicorn` → 502 降级页面 |
| --deploy 检查 | `python manage.py check --deploy` |
| CSRF/SSL/TLS | settings.py 中 SECURE_PROXY_SSL_HEADER / CSRF_TRUSTED_ORIGINS |
| 备份 | `crontab -l` 确认每日备份 + 保留 30 天 |

---

### Type F — 数据迁移审计

**覆盖文件：** `docs/_archive/migration_audit/` + `server/scripts/dump_data.py` + `server/scripts/load_data.py`

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| F1 | `02-数据/数据库结构设计.md` | 新旧表结构对比 |
| F2 | `落地实施/Phase-1.2-题库数据迁移.md` | 迁移方案和步骤 |
| F3 | `docs/_archive/migration_audit/` | 审计文件 — 逐份核对行数和字段 |

---

### Type G — 全面审计（适用于所有审计，作为补充）

**适用范围：** 全项目横切 — 不特定于文件夹

**必须读取的设计文档：**

| # | 文档路径（基于 docs/） | 用途 |
|---|---------|------|
| G1 | `07-工作流/开发工作流程.md` | 开发流程和同步规则 |
| G2 | `00-落地计划.md` | 整体计划与当前阶段定位 |
| G3 | `测试策略.md` | 8 层测试金字塔定义 |
| G4 | `验收检查清单.md` | 项目验收标准 |
| G5 | `备份方案.md` | 备份策略检查 |

---

## 四、审查四步骤

### 第 0 步：读设计文档 → 提取陈述清单

1. 按审计类型（A-G）查上表，确定该读哪些文档
2. 批量读取全部文档
3. 输出清单，每项标注来源文档编号（如 `C4: exam_auto.html §筛选区 — 5 段筛选面板`）

### 第 1 步：拿清单对比计划

| 清单项 | 计划预期 | 结论 |
|--------|---------|------|
| ✅ 计划说已完成 | → 标记为"待代码验证" |
| ⬜ 计划说未开始 | → 标记为"延期" |
| ❌ 计划与设计矛盾 | → 标记为"计划-设计不一致" |

### 第 2 步：拿清单对比代码

分四个阶段执行：

**Stage 1 — 组件探针：** 每个类/方法/文件/路由存在吗？签名正确吗？（快速扫描，不深入）

**Stage 2 — 数据流追踪（强制 R2）：** 对设计文档描述的每个流程，追踪每步输出→输入的消费链。

```
Source → Step1 → Step2 → Step3 → Sink

在每个箭头处检查：
  └─ {上一步} 的输出被 {下一步} 捕获了吗？
  └─ 返回值赋值了吗？(var x = f() vs f())
  └─ 数据类型在定义文件之外被引用了吗？
  └─ 有 UI 组件或持久化层消费它吗？
```

**Stage 3 — 深度探测（强制 R1/R3/R4）：** 对 Stage 1 发现的每个组件，按各类型的逐文件夹 probe 表做深度检查。

**Stage 4 — UI vs HTML 原型比对（仅 Type C）：** 打开 HTML 原型 → 提取 UI 元素清单 → 打开 Flutter Widget → 逐项比对布局/文字/绑定/交互/空态。

---

## 五、力度明文规定（R1-R6）

### R1：每个 verdict 必须附带 concrete evidence

| verdict | 所需证据 | ❌ 禁止写法 | ✅ 强制格式 |
|---------|---------|------------|------------|
| 组件存在 | 文件路径 + 关键代码行 | `QuestionDao 存在 ✅` | `data/daos/question_dao.dart:9-11 — const QuestionDao(this._db)` |
| 流程工作 | 每步箭头的输出→输入链 | `onAppStart 调了 checkAll ✅` | `sync_manager.dart:55 — 调了但返回值丢弃 ❌` |
| 测试通过 | 实际运行测试套件 | `全部通过 ✅` | `pytest -v — 21 passed ✅` |
| 设计符合 | 设计文档 + 代码逐字段对比 | `模型一致 ✅` | `数据库结构设计.md §3.1 vs models.py — 全部字段匹配 ✅` |

### R2：数据流追踪是强制性的

对设计文档描述的每个流程，逐箭头的输出→输入验证。

### R3："存在 ✅" 不是最终结论，是中间状态

每个"存在"必须继续判定为"工作正常"/"已断裂"/"不完整"。

### R4：各层最小探测深度

见各类审计的逐文件夹 probe 表。

### R5：测试结果必须真实运行

不信任执行日志、commit message、计划文档的状态声明。

### R6：每个 async 方法必须检查返回值捕获

`await f()`（丢弃返回值）vs `var r = await f()`（捕获）。

---

## 六、四维放大镜（每个审计必须覆盖全部四个维度）

| # | 维度 | 核心问题 | 具体检查 |
|---|------|---------|---------|
| ① | **设计合规** | 代码与设计文档逐行对照？API 字段名和格式一致？ | 模型逐字段比对、API 响应 schema 比对、Serializer 接受但视图不保存的字段、UI 与 HTML 原型逐元素比对 |
| ② | **测试覆盖** | 每步必有测试？边界场景覆盖？ | 运行测试套件、检查测试文件是否存在、检查边界场景（空/不存在/无效/异常） |
| ③ | **边界异常** | 坏路径不崩？ | 断网/过期/非法输入/并发 4 类场景至少各有一个 |
| ④ | **敷衍应付** | 识别简化/v1/stub/TODO/预留？识别流程断裂？ | 扫描 stub 模式 + 返回值链追踪（R2/R6） |

**敷衍应付识别清单：**

| 模式 | 含义 | 风险评估 |
|------|------|---------|
| `// v1 方案` | 作者知道不完整 | 后续可能永远不来 |
| `// 极简版本` | 故意简化 | 检查切掉了什么：异常路径？边界情况？ |
| `// 预留` | 标记未来实现 | 当前代码错误地工作 |
| `// 待接入` | 依赖外部变更 | 依赖可能永远不实现 |
| `return []` / `return 0` | stub 返回默认值 | 调用方以为"没有数据" |
| `throw UnimplementedError` | 生产路径没构建 | 用户触发即闪退 |
| 注释说算法 X，代码做 Y | 静默简化 | 用户看到错误输出但没有报错 |
| `await f()` 返回值丢弃 | 流程断裂 | 组件存在但断开的伪实现 |

**判断 5 问：**
1. 简化切掉了异常路径吗？
2. 简化切掉了边界情况吗？
3. "后续"有具体的计划和负责人吗？
4. stub 返回了看起来合理的默认数据吗？
5. 简化在最终输出中可见吗？

---

## 七、六维框架（完整报告用）

| 维度 | 一句话本质 | 最低门槛 |
|------|-----------|---------|
| ① 设计合规 | 代码和设计文档对得上吗？ | models.py 与数据库设计.md 逐字段核对 |
| ② 数据正确 | 迁移/构建/同步的数据一条不丢？ | Audit 文件可阅，随机抽 3 条手工核对 |
| ③ 测试覆盖 | CI 上有测试看着吗？边界测了没？ | 该层测试全通过，关键边界场景有测试用例 |
| ④ 异常处理 | 不是理想路径走得好不好，而是坏路径崩不崩？ | 断网/过期/非法/并发 4 类异常至少各有一个场景处理了 |
| ⑤ 用户体验 | 真实教师觉得好用吗？ | 不是你觉得，是教师签字说了算 |
| ⑥ 安全部署 | 上线后不会半夜被叫醒吧？ | --deploy 检查通过、Sentry 有日志、备份就位 |

---

## 八、常见陷阱

| # | 陷阱 | 说明 |
|---|------|------|
| 1 | 执行日志已过时 | Fix 提交更新了代码但没更新执行日志。永远以实际文件和测试结果为准 |
| 2 | 计划状态已过时 | 计划文档的"已完成"/"未开始"标签可能无效。探测真实系统状态 |
| 3 | "我觉得不是阻塞"无效 | 只有验收检查清单决定阻塞等级。发现的问题全部呈现，由 Owner 判断 |
| 4 | "代码看起来很完整"最危险 | 绕过方法论。即使结果看起来明显也要走流程 |
| 5 | 冷却时序违规 | 冷却写入必须在所有操作成功后执行（R6） |
| 6 | 硬编码代替 SystemConfig | 设计文档要求从 assets.db 读取的参数，不在代码中写死 |
| 7 | 回调函数注册了但从未触发 | 函数作为参数传进去了，但没有任何代码调用它 |
| 8 | 忽略 HTML 原型 | 只读文字设计文档（页面设计说明.md）不读 HTML 原型 → 漏审 UI 差异 |
| 9 | 配置文件盲点 | `pubspec.yaml`、`.env`、`nginx.conf` 等声明式配置文件不在 `lib/`、`server/` 等代码文件夹内，容易被全覆盖矩阵遗漏。代码层 `rootBundle.load('assets/...')` 看起来完整正确，但声明文件缺失则运行时闪退。每次审计必须从代码中提取所有外部资源引用路径，反向验证声明文件中的对应声明存在 |
| 10 | 文件夹遗漏 | 审计前未确认覆盖范围就开审，漏掉某些子文件夹 |

---

> 版本：3.0 | 最后更新：2026-07-11
> 对应 Hermes skill：`project-owner-acceptance`
