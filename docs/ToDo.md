# 章鱼智学 v2 · 总任务

---

## API

- 摸底旧版 Django API（列出端点、字段名、认证方式）
- 对照 `服务器_数据库结构.md` 输出字段映射表
- 设计统一响应格式 + 版本控制 + 分页规范
- 设计认证方案（注册/登录/token 存储/刷新/登出）
- 旧版新增：软件版本号检查接口 `GET /api/sync/app-version/`
- 旧版新增/确认：批量推送接口 `POST /api/sync/push/`

## 同步引擎

- 审核 `spec/sync/` 四个设计稿
- 查找社区已有同步方案（判断是否值得引入）

## UI

- 同步队列页面（待同步/失败记录/手动重试）
- 软件版本号显示
- 版本更新弹窗（启动时对比版本号，落后弹窗）

## 技术调试

- LaTeX + Markdown 渲染（选方案、调公式排版、中文混排）
- PDF 生成（选方案、排版、公式处理）
- 图片存储方案（来源、路径、体积估算、缓存策略）

## 测试

- 设计测试流程（单元/集成/UI/离线/多设备）
