# 生产备份恢复演练（2026-07-27）

## 范围

- 服务器：`82.157.115.219`
- 备份：`/var/backups/zhangyuzhixue-v2/pre-deploy/db.20260727-111702.sqlite3.gz`
- 演练方式：在独立 `/tmp/zhangyuzhixue-restore-drill-20260727` 目录恢复
- 临时服务：Gunicorn `127.0.0.1:18001`
- 生产服务和生产数据库：未停止、未修改

## 结果

| 检查项 | 结果 |
| --- | --- |
| gzip 完整性 | 通过 |
| 恢复后数据库大小 | 3,293,184 字节 |
| 迁移前 `PRAGMA integrity_check` | `ok` |
| 迁移前外键异常 | 0 |
| `accounts.0007_registration_consent` 重放 | 成功 |
| Django system check | 通过 |
| 临时 Gunicorn HTTP 冒烟 | 200 |
| 迁移后 `PRAGMA integrity_check` | `ok` |
| 迁移后外键异常 | 0 |
| 从开始恢复到可服务 | 约 3 秒 |
| 临时进程与目录清理 | 完成 |

## 结论

上线前备份能够独立恢复、迁移并启动应用，可作为当前版本的可用恢复点。3 秒是小型本地服务器演练数据，不包含故障确认、备份选择、DNS 或人工决策时间；实际恢复目标应预留更多操作时间。

## 自动脚本增强验证

同日对每周自动恢复脚本的增强候选进行了第二次隔离验证。候选脚本没有覆盖生产正式文件，仅从 `/tmp` 以 `ubuntu` 身份执行。

| 检查项 | 结果 |
| --- | --- |
| 使用备份 | `db.20260727.sqlite3.gz` |
| 恢复表数量 | 52 |
| migration 数量 | 61 -> 63 |
| Django system check | 0 项问题 |
| 随机本地端口 | 自动分配，仅监听 `127.0.0.1` |
| 隔离版本 API | HTTP 200，`code=0` |
| 恢复数据库版本 | schema 1 / data 26 |
| 迁移前后完整性 | `ok`，外键异常 0 |
| 临时进程与目录 | 已清理 |

候选脚本进入生产正式路径后，仍需观察下一次周日 05:00 cron 日志，才能确认 cron 环境本身的首次自动执行。
