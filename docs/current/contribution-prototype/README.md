# 投稿功能静态设计稿

直接使用浏览器打开 `index.html`。设计稿无外部依赖，不连接真实接口，不会修改数据。

本稿确认的产品边界：

- 新题以外部 AI JSON 导入为主要流程。
- 平台负责解析、核对、补充信息、审核和入库。
- 暂不提供图片上传、平台 OCR 和完整手工 LaTeX 录题。
- 纠错仍使用轻量表单，不要求 AI JSON。

左侧导航可切换所有关键页面，右上角可以切换桌面和窄屏预览。
